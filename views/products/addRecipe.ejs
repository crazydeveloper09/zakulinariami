<%- include("../partials/header") %>

<div class="container customContainer">
    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-12">
            <div class="card">
                <div class="card-title text-center title_new" id="name">
                    <br>
                    Powiąż przepis z produktem <%= product.title %>
                </div>
                <div class="card-body">
                    <% if(error && error.length > 0){ %>
                        <div class="alert alert-danger" role="alert">
                            <%= error %>
                        </div>
                    <% } %>
                        <form action="/products/<%= product._id %>/recipes" method="post">
                            <p class="description_new">Wybierz przepis</p>
                            <% if(recipes.length === 0){ %>
                                <div class="row justify-content-center">
                                    <div class="col-lg-6 col-md-12">
                                        <div class="alert alert-secondary text-center" role="alert">
                                       Powiązałeś wszystkie przepisy z tym produktem
                                        </div>
                                    </div>
                                </div>
                                <input type="submit" value="Powiąż przepis z produktem" class="btn btn-outline-dark" disabled>
                            <% } else { %>
                                <select name="recipe" id="select" class="form-control" required>
                                    <% for(let i=0;i< recipes.length;i++){ %>
                                        
                                            <option value="<%= recipes[i]._id %>"><%= recipes[i].title %></option>
                                       
                                       
                                       
                                       
                                        
                                    <% } %>
                                </select>
                                <br>
                                
                                <input type="submit" value="Powiąż przepis z produktem" class="btn btn-outline-dark">
                            <% } %>
                           
                            
                            
                        </form>
                </div>
            </div>
            
        </div>
    </div>
</div>

<%- include("../partials/footer") %>